# Lessons

- Project deployment target for this repository is `amp.you-box.com` on the `jpdata` server, not the shared `api.xiaomao.chat` gateway. Check project-specific deployment notes before assuming a host from global VPS inventory.
